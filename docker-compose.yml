services:
  tamagochi-data:
    build:
      context: .
      dockerfile: data_service/Dockerfile
    container_name: tamagochi-data
    restart: unless-stopped
    env_file: .env
    volumes:
      - persistent_data:/data
    healthcheck:
      test: ["CMD", "python", "-c", "import json,sys; d=json.load(open('/data/status.json')); sys.exit(0 if d.get('state') in ('running','starting') else 1)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s

  tamagochi-bot:
    build: .
    container_name: tamagochi-bot
    restart: unless-stopped
    stop_grace_period: 60s
    env_file: .env
    depends_on:
      tamagochi-data:
        condition: service_healthy
    command: ["python", "-u", "trading_bot.py", "--testnet", "--data-service", "--data-dir", "/data", "--threshold", "0.70", "--amount", "10", "--leverage", "20"]
    volumes:
      - ./trading_logs:/app/trading_logs
      - ./trading_state:/app/trading_state
      - persistent_data:/data:ro

volumes:
  persistent_data:
